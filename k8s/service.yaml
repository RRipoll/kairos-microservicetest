apiVersion: v1
kind: Service
metadata:
  name: price-service
  labels:
    app: price-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: price-service
